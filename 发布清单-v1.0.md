# 发布清单（v1.0）· red-army-story 红军长征叙述性网站

> 版本：v1.0 ｜ 发布日期：2026-09-25 ｜ 维护人：莱桑
> 配套文档：回滚手册.md（出事时按它走）、red-army-story/skills/precheck/（检查脚本）

## 一、版本标记

| 项 | 值 |
|---|---|
| 版本号 | v1.0 |
| 对应提交 | 7c26701（Day25:Bug复现Skill，git tag v1.0） |
| 前端形态 | index.html + assets/ 十幕插画（红金史诗调性 + 粒子层 + 渲染增强包） |
| 后端 | CloudBase 云函数 api-health / api-scenes / api-messages + PostgreSQL 两表 |
| 公网地址 | 前端 https://red-army-story-d5gdwz8vn0d35dc0b-1494694575.tcloudbaseapp.com/index.html ｜ 接口 https://red-army-story-d5gdwz8vn0d35dc0b.service.tcloudbase.com/api/* |

打标命令（发布时执行）：`git tag v1.0 && git push --tags`

## 二、发布前验证项（全部通过才准发布）

| # | 验证项 | 命令 / 方法 | 通过标准 |
|---|---|---|---|
| 1 | 工作区干净 | git status --short | 无未提交改动（发布内容=已提交内容） |
| 2 | 发布前检查 Skill | node red-army-story/skills/precheck/check.js | 6/6 通过，退出码 0 |
| 3 | 密钥红线 | 同上（含在 precheck 内）；git log --all -S 深扫 | 0 特征词，.env 未跟踪 |
| 4 | 文档同步 | api-contract.md 与实际接口一致；回滚手册最新 | 已更新 |

## 三、发布步骤

| 步骤 | 命令 | 预期 |
|---|---|---|
| 1. 部署静态托管 | `MSYS_NO_PATHCONV=1 tcb hosting deploy ./red-army-story/` | 全部文件√，输出 Deployment completed |
| 2. 部署云函数（有改动时） | `MSYS_NO_PATHCONV=1 tcb fn deploy <函数名> --path /api/<路径> --force` | Deployment completed |
| 3. 记录发布耗时 | 前后 date +%s 差值 | 留档（v1.0 实测全量 29 秒） |
| 4. 等 CDN 生效 | sleep 60–75 | no-cache 请求可见新内容 |

## 四、发布后验证

| # | 验证项 | 方法 | 通过标准 |
|---|---|---|---|
| 1 | precheck 全量 | node red-army-story/skills/precheck/check.js | 6/6（含公网首页、三类接口、密钥） |
| 2 | 新版特征确认 | curl -H "Cache-Control: no-cache" …/index.html \| grep -c "<本版独有特征>" | 命中数与预期一致（如 v1.0 的 esc(m.ch = 1） |
| 3 | 浏览器实拍 | 无痕窗口打开 + 截图（带地址栏） | 页面正常渲染、无控制台新报错 |

## 五、代码与数据备份方式

| 对象 | 方式 | 恢复途径 |
|---|---|---|
| 代码 | git commit + push（GitHub 远端）；发布提交打 tag | git show <tag/提交>:<路径> 提取任意历史版本 |
| 密钥 | 只存 cloudfunctions/*/.env（gitignore 排除，永不入库）；本地留存 | 重置 Admin API Key 后更新 .env 并重部署 |
| 数据库 scenes | db/schema.sql + seed 脚本 = 唯一真源，可幂等重放 | 重跑 seed（ON CONFLICT DO UPDATE） |
| 数据库 messages | 软删除设计（is_deleted），include_deleted=1 可见全部 | UPDATE is_deleted=false 找回；控制台备份点兜底 |
| 托管文件 | 当前部署即 git 某提交的镜像；历史在 git | 回滚手册第一节单文件重部署 |
