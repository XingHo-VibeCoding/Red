# 回滚手册（Day 26 起生效）

> 用途：发布出事时，按本手册把线上恢复到已知良好状态。
> 原则：**先回滚、后排查**——恢复服务优先于找根因；回滚不动 git 历史，只动部署层。

## 〇、什么情况必须回滚（判断表）

| 信号 | 严重度 | 动作 |
|---|---|---|
| 公网首页打不开 / 白屏 / HTTP 非 200 | 红线 | **立即回滚**，再排查 |
| precheck 任一项 FAIL（跑 `node red-army-story/skills/precheck/check.js`） | 红线 | 禁止发布；已上线的**立即回滚** |
| 页面能开但核心功能坏（叙事滚不动、接口全挂） | 高 | 15 分钟内修不好就回滚 |
| 数据被误写/误删（scenes 幕内容错乱） | 高 | 走「数据恢复」节，同时考虑回滚前端展示 |
| 个别非核心交互小毛病（如某按钮样式错位） | 低 | **不回滚**，记录进下一轮修复 |
| 仅影响个别旧浏览器/小屏的显示 | 低 | 不回滚，加兼容修复 |

判断口诀：**红线即回滚，高优 15 分钟，低优记下来**。

## 一、静态托管回退（√ 已演练，2026-09-25）

**前置**：git 历史即版本库，任一提交的 index.html 都可提取重部署；assets 文件名稳定，通常只需回滚 index.html 单文件。

```bash
# 1. 提取目标版本的 index.html（git 只读，不改历史）
git show <目标提交>:red-army-story/index.html > /tmp/rollback-index.html

# 2. 部署单文件覆盖线上（Git Bash 需加 MSYS_NO_PATHCONV=1）
MSYS_NO_PATHCONV=1 tcb hosting deploy /tmp/rollback-index.html index.html

# 3. 验证：特征 grep（新旧版本各选一个独有标记）+ precheck
curl -s -H "Cache-Control: no-cache" https://…tcloudbaseapp.com/index.html | grep -c "<旧版独有特征>"
node red-army-story/skills/precheck/check.js
```

**演练实测耗时（2026-09-25，Day 26）**：

| 步骤 | 耗时 |
|---|---|
| 发布 v1.0（18 文件全量，commit 7c26701） | 29 秒 |
| 回滚到 66ff390 旧版（单文件） | **11 秒** |
| 恢复 v1.0（单文件） | 4 秒 |

（另需等 CDN 生效，实测约 60–75 秒后特征可验证；用户侧可硬刷新。）

**恢复 v1.0**：重部署当前工作区版本即可（`tcb hosting deploy ./red-army-story/index.html index.html`），然后 precheck 必须 6/6。

## 二、云函数回退（备查，未实操）

云函数当前**无版本化部署习惯**，回退手段按可用度排序：

1. **本地历史源码重部署**（首选，等同静态托管思路）：`git show <好提交>:cloudfunctions/api-messages/index.js > 临时目录`，连同 db.js、package.json 组成完整函数目录后 `tcb fn deploy api-messages --path /api/messages --force`。耗时约 1–2 分钟。
2. **控制台版本回退**：CloudBase 控制台 → 云函数 → 函数详情 → 函数代码有历史版本记录（保留最近若干次部署），可选版本回滚。
3. **防患**：以后每次改云函数前，先把当前线上能用的版本在本地打 tag 或记录提交号——回退目标要**事先**确定。

验证：`curl` 打对应接口，核对 `{ok:true}` 与中文错误路径（如 PUT 返回 405 中文）。

## 三、数据恢复（备查，未实操）

- **messages（留言墙）**：删除是软删（is_deleted=true），`GET /api/messages?include_deleted=1` 可见全部，把误删行 `UPDATE messages SET is_deleted=false`（用 `cloudbase_postgres` 角色走网关）即恢复；彻底误写的行可 PATCH 修正内容。
- **scenes（十幕叙事，只读消费）**：数据以 `db/schema.sql` + seed 为唯一真源，误改后**重跑 seed 幂等脚本**即可恢复（scenes=ON CONFLICT DO UPDATE）；单幕微调也可走网关 UPDATE 单字段。
- **兜底**：CloudBase 控制台 PostgreSQL 有自动备份（免费版保留有限天数），最坏情况从控制台备份点恢复；恢复后必须 `SELECT count(*)` 闭环 + precheck 全绿。

## 四、演练记录

| 日期 | 内容 | 结果 |
|---|---|---|
| 2026-09-25（Day 26） | 发布 v1.0 → 回滚至 66ff390 → 恢复 v1.0 | 回滚 11 秒生效、页面正常（HTTP 200/83886 字节）；恢复 4 秒、precheck 6/6。全程未动 git 历史 |

## 五、演练记录模板（每次演练复制一份填写）

```
演练日期：
演练人：
演练范围：静态托管 / 云函数 / 数据恢复

【第 0 步 · 确认可回退历史】
做了什么：git log --oneline 确认 v 版本提交号与回滚目标提交号
关键信息：当前版本 = ______ ｜ 回滚目标 = ______
耗时：____ 分 __ 秒 ｜ 结果：目标提交存在且内容可提取 □

【第 1 步 · 发布当前版本】
做了什么：tcb hosting deploy（全量/单文件）______
耗时：____ 秒 ｜ 结果：Deployment completed □ / precheck ___/6 □

【第 2 步 · 退回上一版】
做了什么：git show <目标提交>:red-army-story/index.html → tcb hosting deploy 单文件
耗时：____ 秒 ｜ 结果：部署成功 □
验证：旧版特征 grep = ___ 处（预期>0）□ ｜ 新版特征 grep = ___ 处（预期 0）□
     HTTP ___ / 字节 ____ ｜ 页面实拍是否正常 □（截图名：______）

【第 3 步 · 恢复 v 版本】
做了什么：重部署当前工作区版本
耗时：____ 秒 ｜ 结果：新版特征回归 □ ｜ precheck ___/6 □

【总结】
全程耗时：约 ____ 分钟 ｜ 是否敢在真实事故中用这套流程：敢/不敢，理由：______
发现的问题与改进：______
```

> 填写示例 = 上面「演练记录」表中 2026-09-25 那一行；三张证据截图见 day26-1-v10.png / day26-2-gitlog.png / day26-3-rolled.png（已入库）。
> 演练指引（照做即可）：① git log --oneline 记下两个提交号 → ② `tcb hosting deploy ./red-army-story/` 发布并 date +%s 计时 → ③ `git show <旧提交>:red-army-story/index.html > 临时文件` 后 `MSYS_NO_PATHCONV=1 tcb hosting deploy 临时文件 index.html` 计时 → ④ 等 75 秒 curl 验证新旧特征 → ⑤ 重部署恢复再验证 → ⑥ 按模板填表。
